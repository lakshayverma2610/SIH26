"use client";

import { ArrowDown, FileText, ScanSearch, MapPinned, BellRing, ShieldCheck, Database, Activity } from "lucide-react";
import { motion } from "motion/react";
import { Reveal } from "./Motion";

const flow=[['Complaint',FileText],['Data processing',Database],['Pattern detection',ScanSearch],['Risk prediction',Activity],['Geospatial analysis',MapPinned],['Actionable alert',BellRing],['Law enforcement action',ShieldCheck]];
export default function ProblemSection(){return <section id="problem" className="section-pad bg-white"><div className="section-shell"><Reveal><p className="eyebrow">The intelligence gap</p><h2 className="section-title">From complaints to actionable intelligence.</h2><p className="section-copy">Traditional workflows record what has already happened. Geo-CashWatch transforms each signal into a forward-looking operational picture.</p></Reveal><div className="mt-16 grid gap-8 lg:grid-cols-[.7fr_1.3fr]"><Reveal className="rounded-2xl border border-slate-200 bg-slate-50 p-7"><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-500">Conventional workflow</p>{['Cybercrime complaint','Data point','Investigation'].map((s,i)=><div key={s}><div className="my-4 rounded-lg border border-slate-200 bg-white p-4 text-sm font-bold text-slate-600">{s}</div>{i<2&&<ArrowDown className="mx-auto text-slate-300" size={16}/>}</div>)}</Reveal><div className="rounded-2xl border border-blue-100 bg-[#f7fbff] p-5 sm:p-8"><p className="mb-7 text-xs font-bold uppercase tracking-[.14em] text-blue-700">Geo-CashWatch intelligence pipeline</p><div className="grid gap-3 sm:grid-cols-2">{flow.map(([label,Icon],i)=><motion.div key={label as string} initial={{opacity:0,x:-18}} whileInView={{opacity:1,x:0}} viewport={{once:true}} transition={{delay:i*.08}} className={`flex items-center gap-4 rounded-xl border bg-white p-4 shadow-sm ${i===6?'border-emerald-200 sm:col-span-2':'border-slate-200'}`}><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-lg ${i===6?'bg-emerald-50 text-emerald-700':'bg-blue-50 text-blue-700'}`}><Icon size={17}/></span><div><small className="block text-[9px] font-bold text-slate-400">0{i+1}</small><b className="text-sm text-navy">{label as string}</b></div></motion.div>)}</div></div></div></div></section>}
