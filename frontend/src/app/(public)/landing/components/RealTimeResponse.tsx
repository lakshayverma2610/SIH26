"use client";

import { Activity, ArrowRight, BellRing, CheckCircle2, MapPin } from "lucide-react";
import { motion } from "motion/react";
import { Reveal } from "./Motion";
const flow=['Event','Analysis','Risk detected','Real-time alert','Officer notified','Action'];
export default function RealTimeResponse(){return <section className="section-pad bg-[#edf3f8]"><div className="section-shell"><Reveal><p className="eyebrow">Real-time response</p><h2 className="section-title">From emerging signal to informed response.</h2></Reveal><div className="mt-14 grid items-center gap-12 lg:grid-cols-[1.1fr_.9fr]"><div className="flex flex-wrap items-center gap-2">{flow.map((s,i)=><motion.div key={s} initial={{opacity:0,x:-10}} whileInView={{opacity:1,x:0}} viewport={{once:true}} transition={{delay:i*.1}} className="flex items-center gap-2"><span className={`rounded-lg border px-3 py-2 text-xs font-bold ${i>2?'border-blue-200 bg-blue-50 text-blue-800':'border-slate-200 bg-white text-slate-600'}`}>{s}</span>{i<flow.length-1&&<ArrowRight size={13} className="text-slate-400"/>}</motion.div>)}</div><motion.article initial={{opacity:0,y:35,scale:.96}} whileInView={{opacity:1,y:0,scale:1}} viewport={{once:true}} transition={{duration:.65}} className="rounded-2xl border border-red-100 bg-white p-6 shadow-2xl shadow-slate-300/50"><div className="flex items-center justify-between border-b border-slate-100 pb-4"><span className="flex items-center gap-2 text-[10px] font-extrabold tracking-[.12em] text-red-600"><span className="relative h-2 w-2 rounded-full bg-red-600"><i className="dot-pulse absolute inset-0 rounded-full bg-red-500"/></span> LIVE INTELLIGENCE ALERT</span><Activity size={16} className="text-red-500"/></div><h3 className="mt-5 text-xl font-semibold text-navy">Potential cash-out hotspot detected</h3><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-lg bg-slate-50 p-3"><small className="text-[9px] font-bold text-slate-500">RISK SCORE</small><b className="block text-2xl text-red-600">91%</b></div><div className="rounded-lg bg-slate-50 p-3"><small className="text-[9px] font-bold text-slate-500">CONFIDENCE</small><b className="block text-2xl text-navy">87%</b></div></div><p className="mt-4 flex items-center gap-2 rounded-lg border border-slate-200 p-3 text-xs text-slate-600"><MapPin size={14}/> Location protected · Authorized view only</p><p className="mt-4 flex items-center gap-2 text-xs font-bold text-amber-700"><BellRing size={14}/> Requires attention</p><div className="mt-5 flex items-center gap-2 border-t border-slate-100 pt-4 text-xs font-semibold text-emerald-700"><CheckCircle2 size={15}/> Duty officer notified</div></motion.article></div></div></section>}
