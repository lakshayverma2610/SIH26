"use client";

import { AlertTriangle, CalendarDays, ChevronDown, MapPin, Radar } from "lucide-react";
import { motion } from "motion/react";
import GeoVisual from "./GeoVisual";
import { Reveal } from "./Motion";

export default function IntelligencePreview(){return <section id="intelligence" className="section-pad overflow-hidden bg-white"><div className="section-shell"><Reveal><p className="eyebrow">Command center preview</p><h2 className="section-title">See the threat before it moves.</h2><p className="section-copy">A unified operational view turns dispersed signals into location-aware priorities. Interface shown with demonstration data.</p></Reveal><motion.div initial={{opacity:0,y:50,scale:.97}} whileInView={{opacity:1,y:0,scale:1}} viewport={{once:true,margin:"-100px"}} transition={{duration:.9}} className="panel mt-14 overflow-hidden rounded-2xl"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-slate-200 px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-navy bg-[#0b1f3a] text-white"><Radar size={16}/></span><div><b className="block text-xs text-navy">COMMAND CENTER</b><small className="text-[9px] font-bold tracking-widest text-emerald-700">● LIVE MONITORING</small></div></div><div className="flex gap-2"><button className="flex items-center gap-2 rounded-md border border-slate-200 px-3 py-2 text-[11px] font-semibold"><CalendarDays size={13}/> Last 24 hours <ChevronDown size={12}/></button></div></div><div className="grid lg:grid-cols-[1fr_290px]"><div className="min-h-[470px] p-3"><GeoVisual compact/></div><aside className="border-t border-slate-200 p-5 lg:border-l lg:border-t-0"><div className="flex items-center justify-between"><b className="text-xs tracking-wide text-navy">ACTIVE ALERTS</b><span className="rounded-full bg-red-50 px-2 py-1 text-[9px] font-bold text-red-700">3 HIGH RISK</span></div>{[['Potential cash-out hotspot','91%','Sector 14 zone'],['Pattern deviation detected','78%','Central district'],['Linked complaint cluster','72%','North corridor']].map((a,i)=><motion.div initial={{opacity:0,x:20}} whileInView={{opacity:1,x:0}} transition={{delay:.5+i*.16}} viewport={{once:true}} key={a[0]} className="mt-4 rounded-xl border border-slate-200 p-4"><div className="flex justify-between"><AlertTriangle size={15} className={i===0?'text-red-600':'text-amber-600'}/><b className={i===0?'text-red-600':'text-amber-600'}>{a[1]}</b></div><p className="mt-3 text-xs font-bold text-navy">{a[0]}</p><p className="mt-2 flex items-center gap-1 text-[10px] text-slate-500"><MapPin size={10}/>{a[2]}</p></motion.div>)}</aside></div><div className="grid grid-cols-2 border-t border-slate-200 md:grid-cols-4">{[['COMPLAINT SIGNALS','8,247'],['ACTIVE PREDICTIONS','126'],['PRIORITY ALERTS','18'],['HIGH-RISK ZONES','07']].map((m,i)=><motion.div initial={{opacity:0,y:12}} whileInView={{opacity:1,y:0}} transition={{delay:.8+i*.1}} viewport={{once:true}} key={m[0]} className="border-r border-slate-200 p-5 last:border-0"><small className="text-[9px] font-bold tracking-wider text-slate-500">{m[0]}</small><b className="mt-2 block text-2xl text-navy">{m[1]}</b></motion.div>)}</div></motion.div></div></section>}
