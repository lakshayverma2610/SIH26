"use client";

import { ArrowRight } from "lucide-react";
import { motion } from "motion/react";
import { Reveal } from "./Motion";
export default function ImpactSection(){const phases=['Reactive investigation','Predictive intelligence','Faster prioritization','Proactive intervention'];return <section className="section-pad bg-white"><div className="section-shell grid gap-14 lg:grid-cols-[.7fr_1.3fr]"><Reveal><p className="eyebrow">Operational impact</p><div className="mt-8 border-l-2 border-blue-700 pl-6"><b className="text-6xl font-semibold text-navy">8,000+</b><p className="mt-3 max-w-xs text-sm leading-6 text-slate-600">Approximate cybercrime complaints reported daily in India.</p></div><p className="mt-5 text-[10px] leading-5 text-slate-400">Indicative public-sector scale context. No model accuracy or outcome claims are represented.</p></Reveal><div><h2 className="section-title !mt-0">Move the operating model forward.</h2><div className="mt-10 grid gap-3 sm:grid-cols-2">{phases.map((p,i)=><motion.div initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} transition={{delay:i*.1}} viewport={{once:true}} key={p} className={`relative rounded-xl border p-5 ${i===3?'border-blue-200 bg-blue-50':'border-slate-200 bg-slate-50'}`}><span className="text-[10px] font-bold text-blue-700">0{i+1}</span><b className="mt-3 block text-sm text-navy">{p}</b>{i<3&&<ArrowRight className="absolute -right-3 top-1/2 z-10 hidden rounded-full bg-white p-1 text-slate-400 sm:block" size={24}/>}</motion.div>)}</div></div></div></section>}
